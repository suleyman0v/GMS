import { CleanZeroDirective } from './clean-zero.directive';

describe('CleanZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new CleanZeroDirective();
    expect(directive).toBeTruthy();
  });
});
