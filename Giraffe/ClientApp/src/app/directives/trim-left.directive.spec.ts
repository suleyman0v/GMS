import { TrimLeftDirective } from './trim-left.directive';

describe('TrimLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimLeftDirective();
    expect(directive).toBeTruthy();
  });
});
