import { PercentLimitDirective } from './percent-limit.directive';

describe('PercentLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentLimitDirective();
    expect(directive).toBeTruthy();
  });
});
